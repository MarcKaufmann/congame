% This file provides LaTeX macros for the styles provided by doc-util.rkt
% These are no-ops for now (they do not actually affect the style of the 
% content in PDF output); they simply prevent errors when rendering the
% docs to LaTeX/PDF.
\newcommand{\terminal}[1]{#1}
\newcommand{\browser}[1]{#1}
\newcommand{\dr-message}[1]{#1}
\newcommand{\mock-textbox}[1]{#1}
\newcommand{\mark}[1]{#1}
